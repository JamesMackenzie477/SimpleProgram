Demonstrates simple arithmetic and various high level concepts in assembly.
The Build.bat file will build the source file into a 64-bit Windows console application via NASM and GoLink.
The Run.bat file will build the source file and launch it with the x64dbg debugger.